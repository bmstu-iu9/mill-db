CREATE TABLE Data (
	id int pk,
	value float,
	weight double
);

CREATE PROCEDURE writeproc_Data (@param1 int in, @param2 float in, @param3 double in)
BEGIN
	INSERT TABLE Data VALUES (@param1, @param2, @param3);
END;